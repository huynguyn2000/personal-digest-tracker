# Python
__pycache__/
*.pyc
.venv/
venv/
.tools/
.env

# SQLite side files (the DB itself IS committed; its journals are not)
data/*.db-wal
data/*.db-shm
data/*.db-journal

# render side artifacts we don't commit
out/digest.json
out/_dashboard_inner.html
out/_digest_inner.html
